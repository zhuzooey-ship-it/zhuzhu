# 测试规则

测试文件应以功能 ID 命名，并关联 `governance/feature-catalog.json`。

优先建立：

- 当前会话识别 DOM fixture
- 跨 Tab 案件 Schema fixture
- `user/list`、`order/vip`、`order/point` 脱敏响应 fixture
- OCR 清晰、模糊、状态栏时间、多时间、缺秒、负金额与边界金额样本

真实会员资料必须脱敏。
